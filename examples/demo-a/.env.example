# Namespace for this environment. Every parallel copy (worktree, agent, issue
# branch) needs a distinct value — it is what keeps containers, volumes,
# networks and hostnames from colliding.
COMPOSE_PROJECT_NAME=demo-a

# Must match DEV_GATEWAY_NETWORK in the gateway's own .env.
DEV_GATEWAY_NETWORK=dev-gateway
