# ============================================================================
# demo-a: reaching this project's database by hostname
# ============================================================================
# The optional third layer. With DEV_GATEWAY_TCP=true on the gateway, this
# makes demo-a's Postgres and Redis reachable on the gateway's shared ports,
# without either of them publishing a port of its own:
#
#   psql "postgresql://demo@demo-a-postgres.localhost:5432/demo?sslmode=require"
#   redis-cli -p 6379 --tls --sni demo-a-redis.localhost get key
#
#   docker compose -f compose.yaml -f compose.dev-gateway.yaml \
#                  -f compose.dev-gateway-tcp.yaml up -d
#
# Note which network the datastores join: `dev-gateway-access`, never the
# shared `dev-gateway` network that carries HTTP. And note the rule is
# HostSNIRegexp, so this file does not need to know the gateway's domain and
# works unchanged on a workstation and on a VPS. See docs/tcp-routing.md.
# ============================================================================

services:
  postgres:
    networks:
      - default
      - dev-gateway-access
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=${DEV_GATEWAY_ACCESS_NETWORK:-dev-gateway-access}"
      - "traefik.tcp.routers.${COMPOSE_PROJECT_NAME:-demo-a}-postgres.entrypoints=postgres"
      - "traefik.tcp.routers.${COMPOSE_PROJECT_NAME:-demo-a}-postgres.rule=HostSNIRegexp(`^${COMPOSE_PROJECT_NAME:-demo-a}-postgres\\..+$`)"
      - "traefik.tcp.routers.${COMPOSE_PROJECT_NAME:-demo-a}-postgres.tls=true"
      - "traefik.tcp.routers.${COMPOSE_PROJECT_NAME:-demo-a}-postgres.tls.options=postgres@file"
      - "traefik.tcp.routers.${COMPOSE_PROJECT_NAME:-demo-a}-postgres.service=${COMPOSE_PROJECT_NAME:-demo-a}-postgres"
      - "traefik.tcp.services.${COMPOSE_PROJECT_NAME:-demo-a}-postgres.loadbalancer.server.port=5432"

  redis:
    networks:
      - default
      - dev-gateway-access
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=${DEV_GATEWAY_ACCESS_NETWORK:-dev-gateway-access}"
      - "traefik.tcp.routers.${COMPOSE_PROJECT_NAME:-demo-a}-redis.entrypoints=redis"
      - "traefik.tcp.routers.${COMPOSE_PROJECT_NAME:-demo-a}-redis.rule=HostSNIRegexp(`^${COMPOSE_PROJECT_NAME:-demo-a}-redis\\..+$`)"
      - "traefik.tcp.routers.${COMPOSE_PROJECT_NAME:-demo-a}-redis.tls=true"
      - "traefik.tcp.routers.${COMPOSE_PROJECT_NAME:-demo-a}-redis.service=${COMPOSE_PROJECT_NAME:-demo-a}-redis"
      - "traefik.tcp.services.${COMPOSE_PROJECT_NAME:-demo-a}-redis.loadbalancer.server.port=6379"

networks:
  dev-gateway-access:
    external: true
    name: ${DEV_GATEWAY_ACCESS_NETWORK:-dev-gateway-access}
